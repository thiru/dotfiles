# Agents

## Important Guidelines

- Never download and install tools onto the system without my explicit permission
- Never run any git commands that modify files in any way (e.g. push, pull, commit, add) unless explicitly asked by the user
