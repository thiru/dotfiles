{
    "height": 10, // Makes it so that it only takes up as much vertical space as needed
    "layer": "top", // Waybar at top layer
    "position": "left", // Waybar position (top|bottom|left|right)
    "spacing": 4, // Gaps between modules (4px)

    "modules-center": ["niri/workspaces", "hyprland/workspaces", "sway/workspaces"],

    // Modules
    "sway/mode": {
        "format": "<span style=\"italic\">{}</span>"
    }
}
